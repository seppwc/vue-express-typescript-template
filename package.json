{
  "name": "vue-express-typescript-template",
  "version": "0.0.1",
  "description": "vue-express-typescript-template",
  "scripts": {
    "dev": "npm run --prefix client dev & npm run --prefix server dev",
    "client:dev": "npm run --prefix client dev",
    "server:dev": "npm run --prefix server dev",
    "postinstall": "npm --prefix client install & npm --prefix server insstall"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
